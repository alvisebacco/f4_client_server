#include <stdio.h>
#include <stdlib.h>

void matrix_lower()
{
	system("clear");
	printf("You cant' build a matrix lower than 5x5!");
	exit(1);
}

void same_memory()
{
	system("clear");
	printf("You can't have same memory segment for player1 and player2!");
	exit(1);
}
